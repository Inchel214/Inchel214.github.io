---
layout: base.njk
title: 首页
---

<section class="hero">
  <div>
    <h1 data-i18n="hero_title">技术 · 思考 · 实践</h1>
    <p class="lead" data-i18n="hero_lead">记录项目、分享后端与工程实践中的知识与思考。专注于AI应用开发，助力职业成长与技术落地。欢迎阅读、反馈与讨论。</p>
    <p><a class="cta" href="#posts" data-i18n="cta_latest">查看最新文章</a></p>
  </div>
  <div class="hero-illus" aria-hidden="true">
    <img src="/assets/img/deep-learning-avatar.svg" alt="深度学习头像" width="240" height="160" />
  </div>
</section>

<section id="projects-home" class="projects-home">
  <h2 class="section-title" data-i18n="sec_projects">项目</h2>
  <hr class="section-divider">
  <p class="lead"><span data-lang="zh">华为会议平板智能导播</span> <span class="actions" data-lang="zh"><a class="lang-toggle" href="https://www.bilibili.com/video/BV11e9zYhEDA/?spm_id_from=333.337.search-card.all.click&vd_source=d3c8ff3acdf214ee3f8d964294d6f2a1">视频：语音跟踪/双人对话</a><a class="lang-toggle" href="https://www.bilibili.com/video/BV1PH7GzBEWH/?spm_id_from=333.1387.upload.video_card.click&vd_source=d3c8ff3acdf214ee3f8d964294d6f2a1">S3导播</a><a class="lang-toggle" href="/posts/2025-11-09-胡说八道/">案例文章</a></p>
  {% if projects and projects.length > 0 %}
    <div class="grid">
      {% for p in projects %}
        <article class="project-card">
          <div class="thumb">
            {% if p.cover %}
              <img src="{{ p.cover }}" alt="{{ p.title_zh }} 封面">
            {% else %}
              <svg viewBox="0 0 100 60" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none" aria-hidden="true">
                <defs>
                  <linearGradient id="projHomeGrad" x1="0" x2="1">
                    <stop offset="0%" stop-color="#6C5CE7" />
                    <stop offset="100%" stop-color="#00B894" />
                  </linearGradient>
                </defs>
                <rect width="100" height="60" fill="url(#projHomeGrad)"></rect>
              </svg>
            {% endif %}
          </div>
          <div class="body">
            <h3 class="title">
              <span data-lang="zh">{{ p.title_zh }}</span>
              <span data-lang="en">{{ p.title_en }}</span>
            </h3>
            <p class="one-liner">
              <span data-lang="zh">{{ p.one_liner_zh }}</span>
              <span data-lang="en">{{ p.one_liner_en }}</span>
            </p>
            <p class="meta">
              <span>{{ p.period }}</span>
              · <span>{% if p.status == 'in_production' %}<span data-lang="zh">已上线</span><span data-lang="en">In Production</span>{% elseif p.status == 'in_progress' %}<span data-lang="zh">进行中</span><span data-lang="en">In Progress</span>{% else %}<span data-lang="zh">实验</span><span data-lang="en">Experimental</span>{% endif %}</span>
            </p>
            {% if p.stack %}
              <div class="badges">
                {% for s in p.stack %}
                  <span class="badge">{{ s }}</span>
                {% endfor %}
              </div>
            {% endif %}
            {% if p.metrics %}
              <div class="metrics">
                {% for m in p.metrics %}
                  <span class="metric-chip">
                    <span data-lang="zh">{{ m.label_zh }}</span>
                    <span data-lang="en">{{ m.label_en }}</span>
                    ：{{ m.value }}
                  </span>
                {% endfor %}
              </div>
            {% endif %}
          </div>
        </article>
      {% endfor %}
    </div>
    <p><a class="read-more" href="/projects/"><span data-lang="zh">查看全部项目</span><span data-lang="en">Browse all projects</span> →</a></p>
  {% else %}
    <p><span data-lang="zh">暂无项目。</span><span data-lang="en">No projects yet.</span></p>
  {% endif %}
</section>

<section id="posts" class="posts-grid posts-strips">
  <h2 class="section-title" data-i18n="sec_posts">最新文章</h2>
  <hr class="section-divider">
  <div class="grid">
    {% for post in collections.posts %}
      <article class="post-card">
        {% set cover = post.data.image or (post.templateContent | firstImageSrc) %}
        {% set cover = cover | normalizeCoverPath(post.url) %}
        <div class="thumb">
          {% if cover %}
            <img src="{{ cover }}" alt="{{ post.data.title or post.fileSlug }} 封面图">
          {% else %}
            <img src="/assets/img/sample-cover.svg" alt="{{ post.data.title or post.fileSlug }} 封面图">
          {% endif %}
        </div>
        <div class="post-info">
          <div class="post-title">
            <h3><a href="{{ post.url }}">{{ post.data.title or post.fileSlug }}</a></h3>
            {% set updated = post.data.updated or post.date %}
            <p class="meta"><span data-i18n="updated_label">更新于：</span>{{ updated | dateLocal("yyyy-LL-dd HH:mm") }}</p>
            {% if post.data.excerpt %}
              <p class="excerpt">{{ post.data.excerpt }}</p>
            {% endif %}
        </div>
        </div>
      </article>
    {% endfor %}
  </div>
</section>

<aside class="sidebar">
  <section id="about" class="card">
    <h3 data-i18n="about_title">关于我</h3>
    <p data-i18n="about_intro">
      你好，我是 Inchel Lee，2020 年从广东工业大学硕士毕业，
      是一名 AI 应用与后端系统的开发者。<br>
      毕业至今一直致力于将视频 AI 技术在会议办公场景下的商用与落地。
      这里记录着我过往的参与过的项目与思考。
      我目标是想参与更有商业价值的项目，用AI技术解决真实世界的问题。
    </p>
  </section>

  <section id="projects" class="card">
    <h3 data-i18n="sec_projects">项目</h3>
    {% if projects and projects.length > 0 %}
      <ul>
        {% for p in projects | slice(0,2) %}
          <li>
            <a href="/projects/">
              <span data-lang="zh">{{ p.title_zh }}</span>
              <span data-lang="en">{{ p.title_en }}</span>
            </a>
            <span class="badge" style="margin-left:6px">{% if p.status == 'in_production' %}<span data-lang="zh">已上线</span><span data-lang="en">Live</span>{% elseif p.status == 'in_progress' %}<span data-lang="zh">进行中</span><span data-lang="en">WIP</span>{% else %}<span data-lang="zh">实验</span><span data-lang="en">Experimental</span>{% endif %}</span>
          </li>
        {% endfor %}
      </ul>
    {% endif %}
    <p><a href="/projects/">查看全部项目 →</a></p>
  </section>

  <section id="tags" class="card">
    <h3 data-i18n="sec_tags">标签</h3>
    {% if collections.topTags and collections.topTags.length > 0 %}
      <div class="tag-badges">
        {% for t in collections.topTags %}
          <a class="tag" href="/tags/{{ t.tag }}/" title="{{ t.tag }} {{ t.count }}">
            {{ t.tag }}<span class="badge">{{ t.count }}</span>
          </a>
        {% endfor %}
      </div>
    {% else %}
      <p data-i18n="tags_empty">暂无标签。</p>
    {% endif %}
    <p><a href="/tags/" data-i18n="tags_view_all">查看全部标签 →</a></p>
  </section>

  <section id="contact" class="card">
    <h3 data-i18n="sec_contact">联系我</h3>
    <p data-i18n="contact_intro">如果你对我的项目感兴趣，或者希望与我合作，请通过以下方式联系我</p>
    <p><img class="icon" src="/assets/img/qqmail.png" alt="邮箱"><span data-i18n="label_email">邮箱：</span>inchel214@qq.com</p>
    <p><img class="icon" src="/assets/img/github.svg" alt="GitHub"><span data-i18n="label_github">GitHub：</span><a href="https://github.com/Inchel214">Inchel214</a></p>
  </section>
</aside>
